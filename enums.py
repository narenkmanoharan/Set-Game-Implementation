class Color:
    """
    Enumeration for the Color attribute - Reduces storage space and promotes more readable code
    """
    blue, green, yellow = range(3)


class Symbol:
    """
    Enumeration for the Symbol attribute
    """
    A, S, H = range(3)


class Shading:
    """
    Enumeration for the Shading attribute
    """
    lower, upper, symbol = range(3)

